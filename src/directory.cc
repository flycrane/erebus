#include "directory.h"

namespace Directory {

        Iterator::Iterator(const Path& path) {

        }

        Iterator::Iterator(const std::string& path) {

        }

        Iterator::~Iterator() {

        }

        Entry* Iterator::next(Entry& e) {
                return nullptr;
        }

}
